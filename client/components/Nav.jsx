import { View } from 'react-native';
import { Heading } from 'native-base';

export default function Nav() {

        return(
        <View style={{padding: 10,}}>
            <Heading style={{left: 10,}} size="xl">Todo-app</Heading>
        </View>
        )
} 